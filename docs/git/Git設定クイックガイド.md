# Git設定クイックガイド

## ⚡ 今すぐやること

### 1. GitHubの情報を確認

1. GitHubにログイン
2. 右上のアイコン → **Settings**
3. 左メニュー → **Emails**
4. メールアドレスを確認：
   - 公開メールアドレスを使う場合：そのメールアドレスを使用
   - 非公開にしている場合：「Keep my email addresses private」の下に表示される `username@users.noreply.github.com` を使用

### 2. Gitの設定（ターミナルで実行）

```bash
# ユーザー名を設定（GitHubのユーザー名に置き換える）
git config --global user.name "あなたのGitHubユーザー名"

# メールアドレスを設定（上で確認したメールアドレスに置き換える）
git config --global user.email "your-email@example.com"
```

### 3. 設定の確認

```bash
# 設定が正しいか確認
git config --global user.name
git config --global user.email
```

### 4. 初回コミット

```bash
git commit -m "feat: 初期セットアップ - タスク整理システム"
```

## 📝 例

もしGitHubのユーザー名が `miyaiyutaro` で、メールアドレスが `yutaro@example.com` の場合：

```bash
git config --global user.name "miyaiyutaro"
git config --global user.email "yutaro@example.com"
```

## ✅ 完了したら

設定が完了したら、`docs/git/Gitセットアップ手順.md` の「2. GitHubでリポジトリを作成」から続けてください。
